<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.alimei.dao.IFeedbackMapper">
	<insert id="saveFeedback" parameterType="FeedbackInfo" useGeneratedKeys="false">
		<![CDATA[
	    insert into feedback (id, userid, content, os, devicemodel, appName, appVersion, createTime, log_name) values (
	    	#{id}, #{userId}, #{content}, #{os}, #{deviceModel}, #{appName}, #{appVersion}, #{createTime}, #{logName})
	    ]]>
	</insert>
</mapper>